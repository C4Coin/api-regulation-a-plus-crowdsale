module.exports = function(job) {
  // Do some heavy work

  return Promise.resolve(result)
}

// // const hello = async ({ data }) => {
// //     // do something

// //     return `Hello ${data}`
// // }

// // module.exports = hello

// const hello = async (job) => Promise.resolve('test')

// module.exports = hello

// // module.exports = function(job){
// //     // Do some heavy work

// //     return Promise.resolve(result);
// //   }
